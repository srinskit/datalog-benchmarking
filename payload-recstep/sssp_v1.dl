EDB_DECL:
arc(src int, dest int, weight int)
id(src int)

IDB_DECL:
sssp2(x int, y int)
sssp(x int, y int)

RULE_DECL:
sssp2(x, MIN(0)) :- id(x).
sssp2(y, MIN(d1 + d2)) :- arc(x, y, d2), sssp2(x, d1).
sssp(x, MIN(d)) :- sssp2(x, d).